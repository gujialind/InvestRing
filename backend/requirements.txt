# InvestRing Backend Dependencies
# Generated from backend/deps/ versions (2026-07-12)
# Runtime dependencies

fastapi==0.136.1
uvicorn[standard]==0.46.0
SQLAlchemy==2.0.49
pydantic==2.13.3
pydantic-settings==2.15.0  # issue #252: GHSA-4xgf-cpjx-pc3j 修复版
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.31  # issue #252: PYSEC-2026-3037/3036/3040 修复版（3040 需 0.0.31）
pymysql==1.1.1  # issue #252: PYSEC-2026-502 修复版

# Data
pandas==3.0.2
numpy==2.4.4
python-dateutil==2.9.0.post0

# HTTP & Network
httpx==0.27.2
requests==2.33.1
urllib3==2.7.0  # issue #252: PYSEC-2026-142/141 修复版
certifi==2026.4.22

# Scheduling
apscheduler==3.11.3

# Database migration
alembic==1.19.1

# External data
tushare==1.4.14
# 钉版原因：1.18.90+ 依赖 jsonpath（仅发布 sdist），构建侧需 Dockerfile
# --no-binary jsonpath 豁免（已加）；钉版防上游后续版本再引入新 sdist 依赖
# 破环镜像构建（issue #170 冒烟门禁发现，#196 引入 akshare 后构建即断）
akshare==1.18.94
lxml==6.1.0
simplejson==4.1.1

# uvicorn dependency
click==8.4.2

# Auth
python-dotenv==1.2.2

# Dev / Testing（issue #172 全锁：以干净环境实际解析版本为准，
# 本地 + CI 全量测试验证通过；升级需配套重验）
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
factory-boy==3.3.3
