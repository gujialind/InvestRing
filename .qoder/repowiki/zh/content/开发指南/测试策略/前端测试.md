# 前端测试

<cite>
**本文引用的文件**
- [package.json](file://frontend/package.json)
- [playwright.config.ts](file://frontend/playwright.config.ts)
- [auth.spec.ts](file://frontend/e2e/auth.spec.ts)
- [auth.setup.ts](file://frontend/e2e/fixtures/auth.setup.ts)
- [tsconfig.json](file://frontend/tsconfig.json)
- [test_business_flows.py](file://backend/tests/e2e/test_business_flows.py)
- [factories.py](file://backend/tests/factories.py)
- [conftest.py](file://backend/tests/conftest.py)
- [requirements.txt](file://backend/requirements.txt)
</cite>

## 更新摘要
**所做更改**
- 更新了端到端测试基础设施现代化配置，确保 TestClient 兼容性修复后前端 E2E 测试能够正常运行
- 新增了业务流测试适配新的平台代码要求和确认交易流程的测试策略
- 完善了 Playwright 认证状态管理和多浏览器支持配置
- 增强了前后端测试集成的最佳实践指导

## 目录
1. [引言](#引言)
2. [测试框架演进](#测试框架演进)
3. [Playwright 端到端测试](#playwright-端到端测试)
4. [项目结构](#项目结构)
5. [核心组件](#核心组件)
6. [架构总览](#架构总览)
7. [详细组件分析](#详细组件分析)
8. [依赖分析](#依赖分析)
9. [性能考虑](#性能考虑)
10. [故障排查指南](#故障排查指南)
11. [结论](#结论)
12. [附录](#附录)

## 引言
本文件为 InvestRing 项目前端的测试策略文档，现采用 Playwright 作为主要测试框架，结合组件测试、Hook 测试、状态管理测试、API 客户端测试、路由测试与表单验证测试。文档涵盖端到端测试配置、多浏览器/设备支持、认证流程测试、测试数据 Mock、测试环境配置与测试覆盖率统计，以及移动端与跨浏览器兼容性测试指导。

**更新** 基于后端测试基础设施重构，前端 E2E 测试已适配新的平台代码要求和确认交易流程，确保前后端测试的一致性和稳定性。

## 测试框架演进
项目从传统的单元测试框架转向 Playwright 端到端测试框架，提供更真实的用户交互体验和更强的测试稳定性。

```mermaid
graph TB
subgraph "传统测试框架"
T1["Jest"]
T2["React Testing Library"]
T3["Mock API"]
end
subgraph "新测试框架"
P1["Playwright"]
P2["E2E 测试"]
P3["真实浏览器环境"]
end
T1 --> P1
T2 --> P2
T3 --> P3
```

**章节来源**
- [playwright.config.ts:1-72](file://frontend/playwright.config.ts#L1-L72)
- [package.json:10-11](file://frontend/package.json#L10-L11)

## Playwright 端到端测试

### 配置概览
Playwright 配置支持多种测试场景和环境：

- **测试目录**: `./e2e` - 所有端到端测试文件存放位置
- **超时设置**: 30秒测试超时，5秒 expect 断言超时
- **重试机制**: CI 环境自动重试2次，本地环境0次
- **并行执行**: 完全并行执行，CI 环境限制1个工作进程
- **报告器**: HTML 和 list 报告器组合使用
- **Web服务器**: 自动启动 Next.js 开发服务器，支持端口复用

### 多项目配置
支持三种测试项目类型：

1. **Setup 项目**: 认证状态设置，登录一次后保存状态供所有测试复用
2. **桌面端项目**: Desktop Chrome 设备配置，使用存储状态进行认证
3. **移动端项目**: iPhone 13 设备配置，同样使用存储状态进行认证

**更新** 通过 setup 项目的认证状态管理，确保了测试的稳定性和执行效率，避免了重复登录操作。

**章节来源**
- [playwright.config.ts:13-72](file://frontend/playwright.config.ts#L13-L72)

### 认证流程测试
提供完整的认证测试套件，包括成功登录、失败登录和未登录重定向：

```mermaid
sequenceDiagram
participant User as "用户"
participant Page as "Playwright Page"
participant Login as "登录页面"
participant Auth as "认证系统"
User->>Page : 访问 /login
Page->>Login : 加载登录表单
User->>Login : 输入用户名和密码
Login->>Auth : 提交登录请求
Auth-->>Page : 返回登录结果
Page->>Page : 验证页面跳转或错误提示
```

**更新** 认证测试已适配新的平台代码要求，确保登录流程与后端测试保持一致。

**章节来源**
- [auth.spec.ts:1-31](file://frontend/e2e/auth.spec.ts#L1-L31)

### 测试脚本集成
通过 npm scripts 集成 Playwright 测试：

- `npm run test:e2e`: 运行所有 E2E 测试
- `npm run test:e2e:ui`: 交互式 UI 模式运行测试

**章节来源**
- [package.json:10-11](file://frontend/package.json#L10-L11)

## 项目结构
前端采用 Next.js 应用程序，核心目录与测试相关的关键点如下：
- 应用层：页面与布局位于 src/app，全局提供者用于注册 React Query 客户端与 Toast 容器。
- 组件层：共享组件与 UI 组件位于 src/components，包含表单、按钮等基础 UI。
- 钩子层：业务逻辑封装在 src/hooks，如认证、查询、变更等。
- 状态层：使用 Zustand 存储认证与 UI 状态。
- 类型层：API 请求/响应类型定义位于 src/types。
- 工具层：Axios 封装的 API 客户端。
- 测试层：Playwright 端到端测试位于 e2e 目录，包含认证流程和业务流测试。

```mermaid
graph TB
subgraph "应用层"
P["Providers<br/>src/app/providers.tsx"]
L["布局/页面<br/>src/app/*"]
end
subgraph "组件层"
C1["共享组件<br/>src/components/shared/*"]
C2["UI 组件<br/>src/components/ui/*"]
end
subgraph "钩子层"
H1["useAuth<br/>src/hooks/useAuth.ts"]
end
subgraph "状态层"
S1["authStore<br/>src/stores/authStore.ts"]
S2["uiStore<br/>src/stores/uiStore.ts"]
end
subgraph "工具层"
U1["API 客户端<br/>src/lib/api.ts"]
end
subgraph "测试层"
T1["Playwright E2E<br/>e2e/*"]
T2["业务流测试<br/>后端集成"]
end
P --> C2
P --> S2
L --> H1
H1 --> S1
H1 --> U1
C1 --> U1
T1 --> L
T1 --> H1
T2 --> T1
```

**图表来源**
- [playwright.config.ts:1-72](file://frontend/playwright.config.ts#L1-L72)
- [auth.spec.ts:1-31](file://frontend/e2e/auth.spec.ts#L1-L31)
- [test_business_flows.py:1-206](file://backend/tests/e2e/test_business_flows.py#L1-L206)

## 核心组件
- **API 客户端**：基于 axios 的统一请求封装，包含请求/响应拦截器、统一错误处理与各模块 API 方法集合。
- **认证 Hook**：封装登录、登出、当前用户查询、修改密码、认证守卫与角色校验。
- **状态存储**：Zustand Store，分别维护认证令牌与用户信息、UI 状态（侧边栏、移动端导航、全局加载、Toast 列表）。
- **提供者**：注册 React Query 客户端与全局 Toast 容器，统一缓存策略与通知展示。
- **表单组件**：负责输入收集、条件字段切换与提交触发。
- **Playwright 测试**：端到端测试框架，支持多浏览器、多设备和认证状态管理。

**更新** 核心组件已适配新的平台代码要求，确保前端测试与后端业务逻辑的一致性。

## 架构总览
下图展示了前端测试关注的交互路径：组件通过 Hook 使用 API 客户端；Hook 依赖 Zustand Store；Providers 注入 React Query 与 Toast；页面路由由 Next.js 管理；Playwright 提供端到端测试环境。

```mermaid
sequenceDiagram
participant UI as "组件/页面"
participant Hook as "useAuth"
participant Store as "authStore/uiStore"
participant API as "api.ts"
participant Provider as "Providers"
participant Router as "Next 导航"
participant E2E as "Playwright 测试"
UI->>Hook : 调用 useLogin/useLogout/useCurrentUser
Hook->>Store : 更新 token/user/UI 状态
Hook->>API : 发起登录/查询/修改密码
API-->>Hook : 返回数据或抛出异常
Hook->>Provider : 触发 Toast 展示
Hook->>Router : 路由跳转
E2E->>UI : 端到端测试执行
E2E->>Hook : 模拟用户交互
E2E->>Store : 验证状态变化
E2E->>API : 验证 API 调用
```

**更新** 架构图已包含后端业务流测试的集成，体现了前后端测试的完整链路。

## 详细组件分析

### 组件测试策略
- **渲染测试**：验证组件在不同 props 下的渲染结果，例如按钮变体、禁用态、图标显示。
- **用户交互测试**：模拟点击、输入、切换等行为，断言状态变化与副作用（如 Toast 显示、路由跳转）。
- **异步测试**：对依赖 React Query 的组件，使用测试适配器等待查询完成。
- **表单测试**：针对复杂表单，验证必填字段、条件字段、数值格式与提交行为。

**更新** 表单测试已适配新的平台代码要求，确保字段验证与后端 Schema 一致。

### Hook 测试策略
- **useLogin**：断言成功时写入 token、设置用户、触发 Toast、跳转；失败时显示错误 Toast。
- **useLogout**：断言清理 token、清空查询缓存、显示提示、跳转。
- **useCurrentUser**：断言启用条件查询、设置用户、缓存时间策略。
- **useChangePassword**：断言成功/失败分支的 Toast。
- **useAuthGuard**：断言已登录/未登录时的路由行为。
- **useRoleCheck**：断言角色判断与权限判定。

### 状态管理测试策略
- **认证状态**：验证登录/登出对本地存储、Cookie、Zustand 状态的影响；断言加载状态与鉴权标志。
- **UI 状态**：验证侧边栏开关、折叠、移动端导航可见性、全局加载、Toast 添加/移除。
- **持久化**：验证部分状态持久化在刷新后的恢复。

### API 客户端测试策略
- **请求拦截器**：断言在客户端环境附加 Authorization 头。
- **响应拦截器**：断言 401 清理 token 并跳转登录页。
- **统一错误处理**：断言错误对象包含 code/status/message。
- **各模块 API**：断言请求方法、URL、参数、分页返回结构、幂等键头等。

**更新** API 客户端测试已适配新的平台代码要求，确保所有涉及平台的 API 调用都包含正确的 platform_code 字段。

### 路由测试策略
- **认证守卫**：未登录访问受保护路由跳转登录；已登录访问登录页跳转仪表盘。
- **路由参数**：验证动态路由参数解析与页面渲染。
- **导航行为**：断言跳转目标与时机（登录成功后跳转、登出后跳转）。

### 表单验证测试策略
- **必填字段**：断言缺失必填字段时阻止提交。
- **条件字段**：买卖切换导致不同字段必填。
- **数值格式**：断言非法数值输入不触发提交或显示错误提示。
- **提交流程**：断言提交成功回调被调用、提交中状态更新、错误 Toast。

**更新** 表单验证测试已适配新的平台代码要求，确保所有平台相关的表单都包含 platform_code 字段的验证。

### Playwright 端到端测试策略
- **认证状态管理**：使用 setup 项目登录一次，保存认证状态供所有测试复用
- **多浏览器支持**：支持 Chromium、Firefox、WebKit 等浏览器
- **多设备支持**：支持桌面端和移动端设备配置
- **交互测试**：模拟真实用户操作，包括表单填写、按钮点击、页面导航
- **断言验证**：验证页面 URL、元素存在性、文本内容、路由跳转
- **业务流测试**：覆盖完整的业务流程，包括申购、调仓、确认等核心功能

**更新** 端到端测试策略已全面适配后端测试基础设施重构，确保前端测试与后端业务逻辑的一致性。

**章节来源**
- [auth.spec.ts:1-31](file://frontend/e2e/auth.spec.ts#L1-L31)
- [auth.setup.ts:1-26](file://frontend/e2e/fixtures/auth.setup.ts#L1-L26)
- [playwright.config.ts:46-70](file://frontend/playwright.config.ts#L46-L70)
- [test_business_flows.py:1-206](file://backend/tests/e2e/test_business_flows.py#L1-L206)

## 依赖分析
- **测试框架与运行时**：Next.js 14、React 18、TypeScript、Playwright。
- **状态管理**：Zustand（轻量、易测试）。
- **数据获取**：@tanstack/react-query（便于缓存与并发控制）。
- **HTTP 客户端**：axios（统一拦截器与错误处理）。
- **UI 组件**：Radix UI + Tailwind 生态，组件稳定、易于断言。
- **端到端测试**：Playwright 提供真实浏览器环境和设备模拟。
- **后端测试**：pytest、httpx（兼容版本约束）、TestClient 修复。

**更新** 依赖分析已包含后端测试基础设施的兼容性修复，特别是 httpx 版本约束以确保 TestClient 正常工作。

```mermaid
graph LR
A["Next.js 应用"] --> B["React Query"]
A --> C["Zustand"]
A --> D["Axios"]
A --> E["UI 组件库"]
B --> F["API 客户端"]
C --> G["认证/UI 状态"]
D --> F
H["Playwright"] --> A
H --> I["真实浏览器环境"]
H --> J["设备模拟"]
K["后端测试"] --> L["httpx 兼容性"]
K --> M["TestClient 修复"]
K --> N["平台代码要求"]
```

**图表来源**
- [package.json:1-51](file://frontend/package.json#L1-L51)
- [requirements.txt:21](file://backend/requirements.txt#L21)
- [conftest.py:240-265](file://backend/tests/conftest.py#L240-L265)

**章节来源**
- [package.json:1-51](file://frontend/package.json#L1-L51)
- [requirements.txt:1-51](file://backend/requirements.txt#L1-L51)

## 性能考虑
- **查询缓存**：合理设置 staleTime、retry 次数，避免频繁重复请求。
- **乐观更新**：在支持的场景先更新 UI 再回滚，提升感知性能。
- **组件拆分**：将昂贵计算与渲染拆分为独立组件，配合 React.memo 与 useMemo/useCallback。
- **资源加载**：图片与字体懒加载，减少首屏阻塞。
- **Playwright 性能**：使用并行执行、重用现有服务器、适当的超时设置。
- **测试隔离**：利用事务隔离和 SAVEPOINT 实现测试间数据隔离。

**更新** 性能考虑已包含后端测试的性能优化策略，特别是数据库连接池和事务管理的最佳实践。

## 故障排查指南
- **登录失败**：检查请求拦截器是否注入 token、响应拦截器是否正确处理 401。
- **Toast 不显示**：确认 Providers 中已挂载 ToastContainer，且 useUIStore 的 addToast 调用链正常。
- **查询未更新**：检查 queryKey 是否一致、enabled 条件是否满足、staleTime 是否过短。
- **表单提交无效**：核对必填字段与条件字段逻辑、数值解析与提交回调。
- **Playwright 测试失败**：检查浏览器启动、页面加载、元素选择器、认证状态文件。
- **CI 环境问题**：验证环境变量、端口占用、网络连接、测试超时设置。
- **后端测试失败**：检查 httpx 版本兼容性、platform_code 字段完整性、确认交易流程。

**更新** 故障排查指南已新增后端测试相关的常见问题和解决方案，特别是 TestClient 兼容性和平台代码要求的处理。

**章节来源**
- [playwright.config.ts:38-44](file://frontend/playwright.config.ts#L38-L44)
- [conftest.py:240-265](file://backend/tests/conftest.py#L240-L265)
- [requirements.txt:21](file://backend/requirements.txt#L21)

## 结论
通过引入 Playwright 端到端测试框架并结合后端测试基础设施重构，InvestRing 项目获得了更真实、更稳定的测试能力。结合原有的组件测试、Hook 测试和状态管理测试，形成了完整的测试金字塔。建议优先覆盖关键业务流程（认证、数据列表、核心表单），并逐步扩展到移动端和跨浏览器场景。

**更新** 经过后端测试基础设施现代化改造，前端 E2E 测试现已完全适配新的平台代码要求和确认交易流程，确保了前后端测试的一致性和可靠性。Playwright 的多项目配置和认证状态管理为复杂的端到端测试提供了强大的基础设施支持。

## 附录

### 测试数据 Mock 与环境配置建议
- **Mock API**：使用 jest.mock 或拦截 fetch/xhr，按模块导出函数进行替换，确保请求 URL、参数与响应结构一致。
- **Mock Store**：对 Zustand Store 使用内存快照或直接注入测试用状态，避免真实持久化副作用。
- **Mock Router**：使用 Next.js 测试工具或自定义 MemoryRouter，控制路由行为与参数。
- **Mock Toast**：在测试中注入空实现或计数器，验证 Toast 的添加/移除次数与内容。
- **Mock Playwright**：使用 fixtures 和 setup 项目管理认证状态，避免重复登录。
- **后端测试数据**：使用 factories.py 提供的工厂函数创建一致的测试数据，确保 platform_code 字段完整性。

**更新** 测试数据 Mock 建议已包含后端测试数据的最佳实践，特别是平台代码要求的处理。

### 测试覆盖率统计
- **建议目标**：组件覆盖率 > 80%，分支覆盖率 > 70%，行覆盖率 > 80%。
- **工具**：Jest 配合 @testing-library/react，使用 --coverage 生成报告。
- **关注点**：Hook 的成功/失败分支、API 错误路径、路由守卫逻辑、表单边界条件、Playwright 测试覆盖率。
- **Playwright 覆盖率**：可通过 Playwright 的代码覆盖率工具或第三方工具集成实现。
- **后端测试覆盖率**：使用 pytest-cov 生成详细的测试覆盖率报告。

### 移动端测试与跨浏览器兼容性
- **移动端**：使用 Playwright 的设备模拟功能，重点验证触摸交互、底部导航、侧边栏折叠与表单输入。
- **跨浏览器**：在 Chrome、Firefox、Safari、Edge 上验证关键功能（登录、列表加载、表单提交），关注 polyfill 与 CSS 兼容性。
- **自动化**：结合 GitHub Actions 或 CI 平台的多浏览器矩阵，执行关键测试套件。
- **设备测试**：利用 Playwright 的真实设备配置，测试不同屏幕尺寸和分辨率下的用户体验。

### Playwright 高级配置
- **报告生成**：HTML 报告提供详细的测试结果可视化，适合 CI/CD 集成。
- **调试模式**：使用 `--debug` 模式进行交互式调试，查看测试执行过程。
- **并行执行**：充分利用多核 CPU，提高测试执行效率。
- **重试机制**：在不稳定环境中自动重试，提高测试成功率。
- **跟踪和视频录制**：失败时自动录制屏幕和网络请求，便于问题诊断。

### 后端测试基础设施现代化
- **httpx 兼容性**：版本约束 `>=0.26.0,<0.28.0` 确保与 starlette 0.35.1 兼容。
- **TestClient 修复**：解决 `Client.__init__() got an unexpected keyword argument 'app'` 错误。
- **平台代码要求**：所有涉及平台的 API 调用必须包含 `platform_code` 字段。
- **确认交易流程**：Trade 模型要求显式传入 `confirm_date` 参数以匹配业务逻辑。
- **工厂函数增强**：factories.py 提供完整的测试数据创建函数，确保数据一致性。

**更新** 新增了后端测试基础设施现代化的专门章节，详细说明 TestClient 兼容性修复和业务流测试适配的具体实现。

**章节来源**
- [playwright.config.ts:25-36](file://frontend/playwright.config.ts#L25-L36)
- [playwright.config.ts:18-23](file://frontend/playwright.config.ts#L18-L23)
- [playwright.config.ts:31-36](file://frontend/playwright.config.ts#L31-L36)
- [requirements.txt:21](file://backend/requirements.txt#L21)
- [factories.py:229-272](file://backend/tests/factories.py#L229-L272)
- [conftest.py:240-265](file://backend/tests/conftest.py#L240-L265)