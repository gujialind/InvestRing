# InvestRing 前端开发文档

> 用途：定义前端技术架构、页面设计和组件规范。
> 核心内容：Next.js + shadcn/ui + TailwindCSS，13个核心页面，移动端/PC端双端独立路由。

---

## 1. 技术架构

### 1.1 技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| 框架 | Next.js 14+ (App Router) | 文件系统路由、Server Components缓存 |
| UI组件库 | shadcn/ui | TailwindCSS原生 |
| 样式方案 | TailwindCSS v4 | 原子化CSS、响应式断点 |
| 状态管理 | Zustand | 轻量简洁 |
| 服务端状态 | @tanstack/react-query v5 | 自动缓存刷新 |
| 图表 | Recharts + Lightweight Charts | 收益曲线 + 净值K线 |
| 动画 | framer-motion | 页面转场、卡片动画 |
| PWA | serwist | 离线浏览、添加到主屏幕 |
| 虚拟滚动 | react-virtual | 长列表优化 |

### 1.2 项目目录结构

```
frontend/
├── app/                          # Next.js App Router
│   ├── m/                        # 移动端路由（显式子路径）
│   │   ├── layout.tsx            # 移动端布局（底部导航）
│   │   ├── login/page.tsx
│   │   ├── dashboard/page.tsx    # 单列卡片流
│   │   ├── investors/page.tsx    # 投资人管理（admin）
│   │   ├── portfolios/page.tsx   # 组合列表
│   │   ├── portfolio/[code]/
│   │   │   ├── page.tsx          # 组合详情
│   │   │   ├── positions/page.tsx    # 该组合的持仓
│   │   │   ├── subscriptions/page.tsx # 该组合的申购赎回
│   │   │   └── trades/page.tsx        # 该组合的交易记录
│   │   ├── products/page.tsx     # 产品管理（admin）
│   │   ├── platforms/page.tsx    # 平台管理（admin）
│   │   └── settings/page.tsx     # 设置（admin）
│   ├── login/page.tsx            # PC端登录
│   ├── dashboard/page.tsx        # PC端首页（多列网格 + 侧边栏）
│   ├── investors/page.tsx        # PC端投资人管理
│   ├── portfolios/page.tsx       # PC端组合列表
│   ├── portfolio/[code]/
│   │   ├── page.tsx              # PC端组合详情
│   │   ├── positions/page.tsx
│   │   ├── subscriptions/page.tsx
│   │   └── trades/page.tsx
│   ├── products/page.tsx         # PC端产品管理
│   ├── platforms/page.tsx        # PC端平台管理
│   ├── settings/page.tsx         # PC端设置
│   ├── api/                      # BFF 接口聚合层
│   ├── layout.tsx                # PC端布局（侧边栏）
│   └── page.tsx                  # 根路由（自动跳转）
├── components/
│   ├── ui/                       # shadcn/ui 组件（共享）
│   ├── charts/                   # 图表组件
│   │   ├── AllocationPie.tsx     # 资产配置饼图（共享）
│   │   ├── NavCurve.tsx          # 净值曲线（PC完整版）
│   │   ├── NavCurveSimple.tsx    # 净值曲线简化版（移动端）
│   │   └── ReturnChart.tsx       # 收益图表（共享）
│   ├── mobile/                   # 移动端专用布局组件
│   │   ├── BottomNav.tsx         # 底部导航
│   │   ├── CardStack.tsx         # 卡片堆叠
│   │   └── ActionSheet.tsx       # 底部弹窗
│   ├── desktop/                  # PC 端专用布局组件
│   │   ├── Sidebar.tsx           # 侧边栏
│   │   ├── DataTable.tsx         # 数据表格
│   │   └── SplitPane.tsx         # 分栏布局
│   └── shared/                   # 跨端共享的业务组件
│       ├── PositionCard.tsx
│       └── TradeForm.tsx
├── hooks/                        # 业务逻辑 hooks（共享）
│   ├── usePortfolio.ts
│   ├── useAuth.ts
│   ├── useTrade.ts
│   ├── usePosition.ts
│   └── useInvestor.ts
├── lib/
│   ├── api-client.ts
│   └── utils.ts
├── stores/                       # 全局状态（共享）
│   ├── authStore.ts
│   └── uiStore.ts
├── styles/
│   └── globals.css
└── public/
    ├── manifest.json
    ├── icon-192.png
    └── icon-512.png
```

### 1.3 路由策略：双端独立路由（显式子路径）

**设计决策**：移动端使用 `/m/` 前缀，PC端使用根路径，两套独立页面。

**URL 映射表**：

| 功能 | PC端 URL | 移动端 URL |
|------|----------|-----------|
| 登录 | `/login` | `/m/login` |
| 仪表盘 | `/dashboard` | `/m/dashboard` |
| 投资人管理 | `/investors` | `/m/investors` |
| 组合列表 | `/portfolios` | `/m/portfolios` |
| 组合详情 | `/portfolio/PORT001` | `/m/portfolio/PORT001` |
| 持仓管理 | `/portfolio/PORT001/positions` | `/m/portfolio/PORT001/positions` |
| 申购赎回 | `/portfolio/PORT001/subscriptions` | `/m/portfolio/PORT001/subscriptions` |
| 调仓交易 | `/portfolio/PORT001/trades` | `/m/portfolio/PORT001/trades` |
| 产品管理 | `/products` | `/m/products` |
| 平台管理 | `/platforms` | `/m/platforms` |
| 系统设置 | `/settings` | `/m/settings` |
| 日志管理 | `/settings/logs` | `/m/settings/logs` |
| 任务管理 | `/settings/tasks` | `/m/settings/tasks` |

**原因**：
- InvestRing 是数据密集型应用，PC 端天然适合多列并排，移动端必须垂直堆叠
- 调仓交易、多组合对比等场景，PC 和移动端最优交互方案差异大
- 两套独立页面，改动隔离，各自独立演化
- 显式子路径确保 URL 明确，便于分享和 SEO

### 1.4 路由匹配策略（Middleware）

```typescript
// middleware.ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(request: NextRequest) {
  const userAgent = request.headers.get('user-agent') || '';
  const isMobile = /Mobile|Android|iPhone|iPad|iPod/i.test(userAgent);
  const pathname = request.nextUrl.pathname;
  const token = request.cookies.get('token')?.value;
  
  // 静态资源、API 不处理
  if (pathname.startsWith('/_next') || pathname.startsWith('/api')) {
    return NextResponse.next();
  }
  
  // 根路径自动跳转
  if (pathname === '/') {
    const target = isMobile ? '/m/dashboard' : '/dashboard';
    return NextResponse.redirect(new URL(target, request.url));
  }
  
  // 手机访问PC路径 → 重定向到移动端
  if (isMobile && !pathname.startsWith('/m')) {
    const mobilePath = '/m' + pathname;
    return NextResponse.redirect(new URL(mobilePath, request.url));
  }
  
  // PC访问移动路径 → 重定向到PC端
  if (!isMobile && pathname.startsWith('/m')) {
    const desktopPath = pathname.slice(2) || '/';
    return NextResponse.redirect(new URL(desktopPath, request.url));
  }
  
  // 未登录且访问非登录页 → 重定向到登录页
  const isLoginPage = pathname === '/m/login' || pathname === '/login';
  if (!token && !isLoginPage) {
    const loginPath = isMobile ? '/m/login' : '/login';
    return NextResponse.redirect(new URL(loginPath, request.url));
  }
  
  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

**说明**：
- Middleware 在每个请求都会执行
- 根据 User-Agent 判断设备类型，自动重定向到对应版本
- 分享链接时，接收方会自动跳转到其设备对应的版本
- Token 存储在 Cookie 中，两端通用

### 1.5 组件复用策略

| 层级 | 复用方式 | 目录 |
|------|----------|------|
| **数据层** | 完全共享 | `hooks/` + `lib/api-client.ts` |
| **状态管理** | 完全共享 | `stores/` |
| **UI 原子组件** | 完全共享 | `components/ui/`（shadcn/ui） |
| **图表组件** | 共享配置，按需加载 | `components/charts/` |
| **业务组件** | 按需共享 | `components/shared/` |
| **布局组件** | 独立实现 | `components/mobile/` vs `components/desktop/` |
| **页面级组件** | 独立实现 | `app/m/` vs `app/`（根目录） |

### 1.6 开发优先级

**并行开发**：移动端和 PC 端同步开发，共享数据层先行。

```
阶段 1：基础设施（共享层）
├── lib/api-client.ts
├── hooks/useAuth.ts, usePortfolio.ts, useTrade.ts
├── stores/authStore.ts
└── components/ui/（shadcn/ui 安装）

阶段 2：并行开发
├── 移动端                    ├── PC 端
│   ├── 登录页                │   ├── 登录页
│   ├── 首页                  │   ├── 首页（多列网格）
│   ├── 组合详情              │   ├── 组合详情（多列并排）
│   ├── 持仓管理              │   ├── 持仓管理（数据表格）
│   └── 交易页                │   └── 交易页（分栏布局）
└── 图表组件（共享）
```

### 1.7 响应式断点

| 断点 | 宽度 | 用途 |
|------|------|------|
| 默认 | <640px | 手机（移动端页面） |
| sm | 640px+ | 大屏手机/小平板 |
| md | 768px+ | 平板竖屏 |
| lg | 1024px+ | 平板横屏/笔记本（PC端页面） |
| xl | 1280px+ | 桌面 |

**注意**：移动端和PC端是独立页面，断点主要用于各页面内部的布局微调。

---

## 2. 设计系统

### 2.1 配色规范

```css
/* 金融场景配色 */
--primary: hsl(222.2 47.4% 11.2%);   /* 深蓝黑 */
--profit: hsl(142.1 76.2% 36.3%);    /* 涨/收益：Green-600 */
--loss: hsl(0 72.2% 50.6%);          /* 跌/亏损：Red-500 */
--nav-flat: hsl(215.4 16.3% 46.9%);  /* 持平 */
```

### 2.2 数字展示

```css
/* 数字右对齐、等宽字体 */
.number-cell { @apply text-right font-mono tabular-nums; }
.profit { @apply text-green-600; }
.loss { @apply text-red-500; }
.amount-large { @apply text-2xl font-semibold tabular-nums; }
.amount-small { @apply text-sm text-muted-foreground tabular-nums; }
```

### 2.3 间距与圆角

| 元素 | TailwindCSS |
|------|-------------|
| 卡片圆角 | rounded-lg (8px) |
| 按钮圆角 | rounded-md (6px) |
| 页面边距 | p-4 ~ p-6 |
| 卡片内边距 | p-4 |

---

## 3. PWA配置

```json
{
  "name": "InvestRing 投资年轮",
  "short_name": "InvestRing",
  "display": "standalone",
  "theme_color": "#0f172a",
  "background_color": "#0f172a",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

**缓存策略**：
- 静态资源：Cache First
- API数据：Network First
- 图片：Cache First

---

## 4. 页面设计

### 4.1 页面结构（13个核心页面）

```
登录页（Login）
├── 用户编码输入框
├── 密码输入框
├── 登录按钮
└── 错误提示（密码错误/账户锁定）

首页（Dashboard）
├── 组合总览卡片（总市值、收益率）
├── 待确认申购/赎回提醒
└── 待确认调仓交易提醒

投资人管理页（admin可见）
├── 投资人列表（编码、姓名、角色、份额汇总）
├── 创建投资人（姓名、角色、密码）
├── 编辑投资人（修改密码、角色）
└── 删除投资人（校验无份额）

组合列表页
├── 筛选器：状态（默认活跃 / 待激活 / 已关闭）
├── 组合卡片列表
│   ├── 名称、净值、收益率（待激活状态不显示净值和收益率）
│   └── 状态标签（active/draft/closed，draft 和 closed 显示标签）
│       ├── active：不显示标签（默认状态）
│       ├── draft：显示"待激活"标签（secondary 色系）
│       └── closed：显示"已关闭"标签（muted 色系）
└── 创建组合按钮（admin可见）

组合详情页
├── 状态显示：待激活 / 活跃 / 已关闭（+ 关闭时间）
├── 待激活组合布局（draft）：
│   ├── 组合基本信息（名称、创建时间）
│   ├── 提示信息："组合尚未激活，请执行首次申购以启动组合。初始净值固定为 1.0000"
│   └── 操作按钮（admin）：首次申购激活 | 删除组合
├── 活跃/已关闭组合布局：
│   ├── 净值曲线图
│   ├── 收益统计（累计/年化/TWR）
│   ├── 持仓列表
│   └── 投资人份额表
├── 操作按钮（admin）：
│   ├── 待激活组合：首次申购激活 | 删除组合
│   ├── 活跃组合：申购 | 赎回 | 关闭组合
│   └── 已关闭组合：重新激活
├── 份额变动事件入口（admin，活跃组合可见）
└── 关闭组合弹窗：
    ├── 待处理交易检查（申购/赎回/调仓/份额变动）
    ├── 投资人份额检查
    └── 持仓状态警告（非现金持仓）

持仓管理页
├── 持仓列表（按资产类别分组）
├── 收益归因
├── 调仓按钮（admin，活跃组合可见）→ 跳转调仓交易页
├── 非净值型资产更新入口（admin）
└── 份额变动事件管理（admin）
    ├── 待确认事件列表
    ├── 创建事件（分红/拆分/合并等）
    └── 批量确认

申购赎回页
├── 待确认申购/赎回列表
├── 历史记录
├── 新建入口（admin可见）
└── 首次申购提示（draft 组合）：
    ├── 页面顶部显示提示条："首次申购将激活组合，初始净值固定为 1.0000"
    └── 提交后跳转回组合详情页，状态变为 active

调仓交易页
├── 待确认交易列表
├── 历史记录
└── 新建入口（admin可见）

产品管理页（admin可见）
├── 产品列表（编码、名称、类型、市场、数据源状态）
├── 新增产品
├── 编辑产品
└── 验证数据源 / 同步历史数据

平台管理页（admin可见）
├── 平台列表
├── 新增平台
└── 删除平台

设置页（admin可见）
├── 数据源配置（Tushare API Key）
├── 交易日历同步
└── 系统状态（最后同步时间）

日志管理页（admin可见）
├── 路径：PC `/settings/logs` | 移动端 `/m/settings/logs`
├── 登录日志 Tab
│   ├── 筛选器：用户、状态、时间范围
│   └── 表格：用户、操作、IP、状态、时间
├── 操作审计 Tab
│   ├── 筛选器：用户、操作类型、资源类型、时间范围
│   └── 表格：用户、操作、资源、变更内容、时间
├── 任务日志 Tab
│   ├── 筛选器：任务、状态、时间范围
│   └── 表格：任务、状态、执行时长、处理记录数、时间
└── 错误日志 Tab
    ├── 筛选器：错误类型、时间范围
    └── 表格：错误类型、错误信息、请求路径、时间

任务管理页（admin可见）
├── 路径：PC `/settings/tasks` | 移动端 `/m/settings/tasks`
├── 任务列表
│   ├── 任务名称、状态、下次执行时间
│   ├── 操作：手动执行、启用/禁用
│   └── 查看执行历史
└── 执行历史
    ├── 执行时间、状态、耗时
    └── 查看详情（净值同步明细）
```

### 4.2 组合详情页布局

```
┌─────────────────────────────────────────────────────────┐
│  组合名称: XXX    净值: 1.2543    总市值: 125.43万       │
│  总份额: 100万份          更新时间: 2026-04-08          │
├─────────────────────────────────────────────────────────┤
│  【收益率切换】 累计收益率 | 年化收益率 | 时间加权收益率   │
├─────────────────────────────────────────────────────────┤
│  【净值曲线】  图表区域                                    │
├─────────────────────────────────────────────────────────┤
│  【持仓列表】                               [+ 新建持仓]  │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 基金名称      份额      成本价   当前净值   收益率   │ │
│  │ 易方达蓝筹    5000份   2.10     2.35      +11.9%  │ │
│  │ 华夏沪深300   3000份   1.50     1.62      +8.0%   │ │
│  │ 现金          5万      -        -         -       │ │
│  └────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│  【投资人份额】                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ 投资人    持有份额    成本价    当前市值   占比      │ │
│  │ 爸爸      40万份      1.00     50.17万    40%      │ │
│  │ 妈妈      35万份      1.00     43.90万    35%      │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 5. 核心组件

### 5.1 持仓列表组件

```tsx
interface PortfolioPositionItem {
  id: number;
  product_code: string;
  product_name: string;
  market: string;
  platform_code?: string;  // 持仓所属平台
  shares: number;
  frozen_shares: number;
  cost_price: number;
  unit_price: number;
  market_value: number;
  profit: number;
  profit_pct: number;
  snapshot_date: string;
}

// 非净值型资产识别
const isNonNavAsset = (position: PortfolioPositionItem) => 
  position.shares === null && position.amount !== null;
// 或者：product_code 为 CASH
```

### 5.2 非净值型资产更新

适用资产：现金类资产（CASH）
不适用：ETF/LOF基金、场外基金

```tsx
// 批量更新接口
PATCH /api/portfolios/{portfolio_code}/positions/non-nav-assets
Body: { 
  updates: [ 
    { 
      product_code,     // 资产代码，如 'CASH'
      amount,           // 新金额
      market: null,     // 现金类资产 market 为 null
      effective_date?   // 生效日期（格式 YYYY-MM-DD，默认当天）
    } 
  ] 
}

// 字段说明
// - product_code: 资产代码（CASH）
// - amount: 新金额
// - market: 现金类资产固定为 null
// - effective_date: 生效日期（格式 YYYY-MM-DD，默认当天）
```

### 5.3 交易录入表单

```tsx
// 买入表单字段
interface BuyForm {
  product_code: string;
  market: 'CN_EXCHANGE' | 'CN_OTC';
  actual_amount: number;  // 实际支付
  fee: number;            // 手续费
  price?: number;         // 成交价（可选）
  trade_date: string;
}

// 计算逻辑
// actual_amount = amount + fee
// shares = amount / price
```

---

## 6. 交互状态

### 6.1 收益率切换

- 默认选中：时间加权收益率（年化）
- 原因：最能真实反映投资管理能力，行业标准

### 6.2 非净值型资产更新流程

```
1. 用户点击持仓列表中的「更新金额」按钮
2. 进入非净值型资产批量更新页面
3. 用户填写新金额
4. 选择生效日期（默认当天，支持选择历史日期）
5. 点击「保存修改」
6. 调用 API：PATCH /api/portfolios/{id}/positions/non-nav-assets
7. 成功后关闭页面，刷新持仓列表
```

### 6.3 添加产品流程

```
1. 用户填写产品信息（代码、名称、类型、市场）
2. 点击"添加产品"
3. 校验数据源：POST /api/products/{code}/{market}/validate-source
4. 同步历史数据：POST /api/products/{code}/{market}/sync-history
5. 添加成功，刷新产品列表
```

**LOF产品**：需分别添加场内和场外两条记录
- 场内：market = CN_EXCHANGE
- 场外：market = CN_OTC

---

## 7. 错误处理

| 错误场景 | 处理方式 |
|---------|---------|
| 网络错误 | Toast：网络连接失败，请重试 |
| 金额格式错误 | 输入框下方红色提示 |
| 更新失败 | Toast显示错误信息 |
| 权限不足 | 隐藏相关入口（viewer角色） |

---

## 8. 认证策略

### 8.1 Token 存储与管理

```typescript
// Token 存储
localStorage.setItem('token', response.token);
localStorage.setItem('token_expires_at', response.expires_at);
localStorage.setItem('user', JSON.stringify(response.user));

// Token 获取
const token = localStorage.getItem('token');
const expiresAt = localStorage.getItem('token_expires_at');

// Token 过期检查
function isTokenExpired(): boolean {
  const expiresAt = localStorage.getItem('token_expires_at');
  return expiresAt ? Date.now() > Number(expiresAt) : true;
}
```

### 8.2 路由守卫

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const token = request.cookies.get('token');
  const pathname = request.nextUrl.pathname;
  
  // 未登录且访问非登录页 → 跳转登录页
  if (!token && pathname !== '/login') {
    return NextResponse.redirect(new URL('/login', request.url));
  }
  
  // 已登录且访问登录页 → 跳转首页
  if (token && pathname === '/login') {
    return NextResponse.redirect(new URL('/', request.url));
  }
  
  return NextResponse.next();
}
```

### 8.3 权限控制

| 用户角色 | 可访问页面 | 可执行操作 |
|---------|-----------|-----------|
| admin | 全部页面（13个） | 全部操作 |
| viewer | 登录页、首页、组合列表/详情、持仓管理（只读）、申购赎回（只读）、调仓交易（只读） | 仅查看，无创建/修改权限 |

**实现方式**：
- 前端根据 `user.role` 隐藏管理入口（按钮、菜单项）
- admin 专属页面：投资人管理、产品管理、平台管理、设置页、日志管理页、任务管理页
- 后端 API 校验角色，viewer 调用管理接口返回 403

### 8.4 登录流程

```
1. 用户输入编码 + 密码
2. 调用 POST /api/auth/login
3. 成功：存储 token + user 信息，跳转首页
4. 失败：显示错误提示
   - 密码错误：提示"用户编码或密码错误"
   - 账户锁定：提示"账户已锁定，请15分钟后重试"
```

### 8.5 Token 过期处理

```typescript
// API 请求拦截器
axios.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      // Token 过期或无效
      localStorage.clear();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 8.6 登出流程

```
1. 调用 POST /api/auth/logout（将 token 加入黑名单）
2. 清除 localStorage（token、user 信息）
3. 跳转登录页
```